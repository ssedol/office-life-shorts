# 오늘도출근 Shorts Factory
#
# 시스템 요구사항 (pip으로 설치되지 않습니다):
#   ffmpeg / ffprobe  — 렌더링에 필수
#       Ubuntu/Debian: sudo apt-get install ffmpeg
#       macOS:         brew install ffmpeg
#       Windows:       https://www.gyan.dev/ffmpeg/builds/
#   한글 폰트         — 자막 렌더링에 필수
#       Ubuntu/Debian: sudo apt-get install fonts-nanum
#       (Windows/macOS는 기본 내장 폰트를 자동으로 찾습니다)

# --- 필수 ---------------------------------------------------------------
edge-tts>=6.1            # 기본 한국어 TTS (명세서 §18)

# --- 권장 ---------------------------------------------------------------
Pillow>=10.0             # 이미지 규격 검증 (§14) + 샘플 이미지 생성 도구
                         # 없어도 동작하지만 이미지 크기/비율 검사를 건너뜁니다

# --- YouTube 업로드 (선택) ----------------------------------------------
# tools/upload.py 를 쓸 때만 필요합니다. 렌더 파이프라인은 이 패키지를 import하지 않습니다.
# 감사를 통과하지 않은 API 프로젝트의 업로드는 유튜브가 비공개로 잠급니다 (DEC-012).
google-api-python-client>=2.100
google-auth-oauthlib>=1.2

# --- 개발/테스트 --------------------------------------------------------
pytest>=8.0
jsonschema>=4.0          # schema/*.json 으로 입력을 검증하는 테스트용
